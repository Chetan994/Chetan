# Book API

Simple REST API for managing a list of books using Node.js and Express.

## Setup

```bash
npm install
npm start
```

## Endpoints

- GET `/books`
- POST `/books`
- PUT `/books/:id`
- DELETE `/books/:id`
